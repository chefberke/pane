/** Group-filter value meaning "no group filter applied". */
export const GROUP_ALL = 'all';
/** Group-filter value meaning "blocks not inside any group". */
export const GROUP_UNGROUPED = 'ungrouped';
/** Type-filter value meaning "no type filter applied". */
export const TYPE_ALL = 'all';

/** Max height (px) of the scrollable results list. */
export const LIST_MAX_HEIGHT = 300;
/** Height (px) of each result row. */
export const ROW_HEIGHT = 40;
/** Height (px) of the search input row. */
export const INPUT_HEIGHT = 48;
/** Height (px) of the footer hint row. */
export const FOOTER_HEIGHT = 32;
/** Height (px) of the empty ("No results") placeholder. */
export const EMPTY_HEIGHT = 80;
/** Height (px) of a filter chip. */
export const CHIP_HEIGHT = 24;
/** Diameter (px) of the color-swatch dot on a group chip. */
export const SWATCH_SIZE = 8;
